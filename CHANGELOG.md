# 更新记录（Changelog）

本文件记录 TransStation 中转站的版本变更，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式约定。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)：`主.次.修订`。

---

## [0.1.0] - 2026-09-07

首个可发布版本。完成“快捷键唤出 → 拖放存入 → 快速取用”核心闭环，
以单文件 exe 分发，可在任何 Windows 10 / 11（64 位）直接运行。

### 新增

- **呼出与收起**
  - 全局快捷键（默认 `Ctrl+Alt+T`，可在设置中改绑），任意应用（含拖拽过程中）可触发；
  - 面板跟随鼠标弹出、始终置顶；支持钉住常驻、手动调整大小与位置记忆；
  - “按一次即够”的等待投放会话：存入后自动收起；约 6 秒无任何活动自动收起；
    再按快捷键或 `Esc` 随时收起；支持系统“减少动画”设置。
- **内容收集**
  - 拖放存入：文件（含多选/文件夹）、文本、网页链接（自动取标题）、图片
    （网页图片自动本地镜像 PNG）；
  - 剪贴板添加：文本 / 链接 / 文件列表 / 图片（兼容 Snipaste 等仅支持复制的应用）；
  - 存入提示带“撤销”按钮；文件条目只记录路径，绝不复制或移动原文件。
- **内容管理**
  - 行卡片列表：类型图标、来源与时间、图片缩略图、置顶角标；
  - 全文搜索（标题/正文/备注/链接）与类型筛选（全部/文件/图片/链接/文本）；
  - 右键操作：打开、复制、复制路径、发送到前台窗口、显示文件位置、修改类型、
    置顶、编辑备注、删除；
  - 回收站：软删除保留 30 天，可恢复或彻底删除（托盘菜单进入）。
- **取用**
  - 卡片拖出到其它应用（文件/文本/链接/图片，按目标应用提供 OLE 格式）；
  - 双击默认动作（文件=系统打开，链接=浏览器，文本=查看/编辑）；
  - “发送到前台窗口”默认写入剪贴板并提示粘贴，可选自动 `Ctrl+V`（默认关闭）。
- **记录与数据**
  - 自动记录来源应用（标题栏“来自 xxx”提示）；取用动作计入本周统计；
  - 忽略清单：指定应用不参与记录（支持通配）；
  - 每日自动备份（默认保留 7 份）；数据目录整体迁移（设置界面）；
  - 全部数据仅存本机 `data/`，便携优先（数据目录跟随 exe）。
- **分发与交付**
  - 单文件 exe（约 48 MB，PyInstaller 自包含，无运行环境依赖）；
  - 深色/浅色系统主题均适配（强制浅色设计令牌体系）；
  - 首次运行三步引导与空状态插图。

### 修复

- 顶层浮窗透明度动画失效导致面板呼出后不可见；
- 深色系统主题下未覆盖控件（右键菜单等）黑底黑字；
- 全局热键注册后消息未被接收（原生事件过滤器未安装）；
- 长按快捷键触发系统自动重复导致面板跟随鼠标闪烁/位移（去抖 + 拖拽中保持）；
- 面板失焦即收起，打断“重新拖入”流程（改为等待投放会话模型）；
- 标题栏来源提示文字被截断（自动省略号 + 悬停全文）；
- 仅携带 `<img src="file:///…">` 的拖拽无法识别（如截图工具）；
- 重复启动时数据库并发建表报错（幂等迁移）；
- 打包重建误删用户数据（现打包前自动保全 `data/`）。

### 已知限制（规划内，见路线图）

- 链接健康检查、历史记录修正、智能建议、规则映射、报表尚未开放（数据结构已预留）；
- 失效链接筛选与“忽略链接检查”菜单项待 M2 接入；
- 深色界面主题未提供（仅预留设计接口）。

### 开发与构建

- 源码运行：`python main.pyw`（Python 3.14 + PySide6 6.11）；
- 自检：`python main.pyw --smoke`、`python -m pytest tests`；
- 打包：`python tools/build_portable.py` → `dist/TransStation/TransStation.exe`；
- 真机验收清单见 `docs/acceptance.md`；协作与提交规范见 `AGENTS.md`。

[0.1.0]: https://github.com/jchj701/TransStation/releases/tag/v0.1.0
